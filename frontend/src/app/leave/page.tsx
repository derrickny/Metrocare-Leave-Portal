import React from 'react';
import Navbar from '@/components/Navbar';

type props = { }

export default function Leave({}: props) {
    return (
        <div>
        <Navbar />
            Leave
        </div>
    )
}